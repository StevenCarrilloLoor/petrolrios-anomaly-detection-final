# .NET
bin/
obj/
dist/

# Carpetas del central generadas al publicar una actualizacion del agente (exe + manifiesto).
# Son artefactos/config de runtime, no fuente: las monta docker-compose.prod.yml.
central-descargas/
central-config/

# Datos de runtime del agente (store-and-forward, watermark, memoria de envio) — no al repo
src/PetrolRios.StationAgent/pending/
**/pending/*.json
**/pending/*.huellas
**/pending/watermark.txt
*.log
*.user
*.suo
*.userosscache
*.sln.docstates
*.userprefs
.vs/
[Dd]ebug/
[Rr]elease/
x64/
x86/
[Aa][Rr][Mm]/
[Aa][Rr][Mm]64/
bld/
[Bb]in/
[Oo]bj/
[Ll]og/
[Ll]ogs/
# Excepción: la carpeta de DTOs "Logs" es CÓDIGO FUENTE (no logs de runtime).
# Sin esto, [Ll]ogs/ ignoraba src/PetrolRios.Application/DTOs/Logs/ y su DTO no entraba al repo.
!src/PetrolRios.Application/DTOs/Logs/
!src/PetrolRios.Application/DTOs/Logs/**
TestResults/
coverage/

# NuGet
*.nupkg
*.snupkg
.nuget/
packages/

# Node / Frontend
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/.env
frontend/.env.local
frontend/.env.*.local

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db
desktop.ini

# Environment / Secrets
appsettings.Production.json
*.pfx
*.key
.env
.env.*
!.env.example

# Coverage reports
coverage*.xml
coverage*.json
*.coveragexml
coverage-results/
coverage-report/

# Firebird: backups grandes y la BD restaurada (datos, no van al repo)
firebird_data/
_arranque/firebird_data/
*.FBK
*.fbk
*.FDB
*.fdb

# Configuracion real del agente por estacion (contiene contrasenas) — nunca al repo
agent-config.json
**/config/agent-config.json
!agent-config.example.json

# Secretos locales (SMTP/App Password) — nunca al repo
**/appsettings.Secrets.json

# Conexión a la base persistida desde Ajustes (puede contener credenciales) — nunca al repo
**/config/connection.json
connection.json

# Config del deploy remoto (servidor SSH, ruta) — local de cada quien
INSTALACION/.deploy.config
